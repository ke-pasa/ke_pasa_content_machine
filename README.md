# ke_pasa_content_machine

Infrastructure helpers now live in this repo to let you run the workers on a standalone VM. See `deploy/README.md` for Docker + Compose deployment, .env secret handling, and systemd/cron scheduling examples that replace GitHub Actions cron jobs.
